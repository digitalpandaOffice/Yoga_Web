# ----------------------------------------------------------------------
# FRONTEND FIREWALL & SECURITY HEADERS
# ----------------------------------------------------------------------

# 1. Disable Directory Browsing
Options -Indexes

# 2. Block Access to Sensitive Files
<FilesMatch "(?i)(\.git|\.env|README\.md|SECURITY_AUDIT_REPORT\.md|SEO_IMPLEMENTATION_REPORT\.md)$">
    Order allow,deny
    Deny from all
</FilesMatch>

# 3. Security Headers
<IfModule mod_headers.c>
    # Protect against clickjacking
    Header set X-Frame-Options "SAMEORIGIN"
    # Protect against MIME sniffing
    Header set X-Content-Type-Options "nosniff"
    # Enable XSS protection filter in browser
    Header set X-XSS-Protection "1; mode=block"
</IfModule>
